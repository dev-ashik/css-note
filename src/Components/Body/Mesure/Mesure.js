import React from 'react';
import Layout from '../../Layout/Layout';

const Mesure = () => {
    return (
        <Layout>
            <h1>Mesure</h1>
            px, pt, cm
        </Layout>
    );
};

export default Mesure;
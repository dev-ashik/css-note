import React from 'react';
import Layout from '../../Layout/Layout';

const Colors = () => {
    return (
        <Layout>
            <h1>here is colors</h1>
        </Layout>
    );
};

export default Colors;